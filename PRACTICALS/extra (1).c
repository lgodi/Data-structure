#include<stdio.h>
void main()
{
       char *str[20],search[10];
       int i=0,c=0;
       printf("\n enter string=");
       gets(str);
       puts(str);
       printf("\n enter a string you want to search");
       gets(search);
       puts(search);
}
