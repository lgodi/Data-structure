#include<stdio.h>
#include<conio.h>
int count(int n);
int main()
{
    int n,ans;
    printf("\n enter number=");
    scanf("%d",&n);
    ans=count(n);
    printf("\n answer=%d",ans);
}
int count(int n)
{
    int c=0;
    while(n>0)
    {

        return n=(n%10)+sum(n/10);
    }
}
